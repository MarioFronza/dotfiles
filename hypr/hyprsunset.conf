# Makes hyprsunset do nothing to the screen by default.
# Without this, the default applies some tint to the monitor.
profile {
    time = 07:00
    identity = true
}

# To enable auto night-light switching, add to hypr/autostart.lua:
#   hl.exec_cmd("hyprsunset")
# and add a second profile here:
# profile {
#     time = 20:00
#     temperature = 4000
# }
