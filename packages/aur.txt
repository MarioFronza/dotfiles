# AUR packages — every name here was confirmed to exist via the AUR RPC API
# (https://aur.archlinux.org/rpc/v5/info) before being listed. Installed by
# install.sh, or manually with:
#   sed 's/#.*//' aur.txt | awk 'NF{print $1}' | xargs yay -S --needed
#

# --- Dev tools ---
claude-code
rtk

# --- Networking (bluetooth TUI, wifi's impala is in pacman.txt) ---
bluetuith

# --- Apps ---
1password
1password-cli
spotify
nordvpn-bin
localsend

# --- Bootloader (Limine + Snapper integration) ---
limine-mkinitcpio-hook
limine-snapper-sync

# --- Misc tools ---
xdg-terminal-exec
cliamp
tensaku
ttf-ia-writer
ufw-docker
tzupdate
yaru-icon-theme

# --- Android / mobile RE ---
android-apktool
android-sdk-build-tools-36

# --- Security / reverse engineering ---
burpsuite
dex2jar
# python-frida-bin: prebuilt PyPI wheel, not python-frida — the source
# build compiles V8 from scratch (single-threaded, 30-90+ min, prone to
# failing on 15GB RAM). Provides python-frida, satisfies frida-tools' dep.
python-frida-bin
frida-tools
