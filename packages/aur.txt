# AUR packages — every name here was confirmed to exist via the AUR RPC API
# (https://aur.archlinux.org/rpc/v5/info) before being listed. Installed by
# install.sh, or manually with:
#   grep -v '^#' aur.txt | xargs yay -S --needed
#
# Hyprland-only AUR packages live in aur-hyprland.txt instead.

# --- Apps ---
1password
1password-cli
spotify
typora
nordvpn-bin
localsend

# --- Bootloader (Limine + Snapper integration) ---
limine-mkinitcpio-hook
limine-snapper-sync

# --- Misc tools ---
xdg-terminal-exec
herdr
cliamp
tensaku
ttf-ia-writer
ufw-docker
tzupdate
yaru-icon-theme

# --- Android / mobile RE ---
android-apktool
android-sdk-build-tools-36

# --- Security / reverse engineering ---
burpsuite
dex2jar
python-frida
python-frida-tools

# --- OBS plugins ---
obs-shaderfilter-git
obs-shaderfilter-git-debug
